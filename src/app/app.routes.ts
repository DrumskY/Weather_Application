import { Routes } from '@angular/router';
import { CityWeatherPageComponent } from './weather/pages/city-weather-page.component';

export const routes: Routes = [
    {
        path: "",
        component: CityWeatherPageComponent
    }
];
